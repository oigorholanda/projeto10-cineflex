import styled from "styled-components"

export default function ListaFilmes() {
    return (
        <Lista>Lista Filmes</Lista>
    )
}

const Lista = styled.ul`
    margin-top: 80px;
    font-family: 'Roboto', sans-serif;
`